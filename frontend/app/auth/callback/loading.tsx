import { Suspense } from "react";

export default function Loading() {
    return (
        <div className="flex text-lg h-screen w-full items-center justify-center font-bold">
            Loading...
        </div>
    );
}
