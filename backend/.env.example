# Ollama Configuration
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:4b

# Database (for future iterations)
DATABASE_URL=postgresql+asyncpg://context:context_dev@localhost:5432/context

# Redis (for future iterations)
REDIS_URL=redis://localhost:6379

# TickTick OAuth (for Iteration 2)
TICKTICK_CLIENT_ID=
TICKTICK_CLIENT_SECRET=
TICKTICK_REDIRECT_URI=http://localhost:8000/api/auth/ticktick/callback

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Secret key for JWT (for future iterations)
SECRET_KEY=your-secret-key-min-32-characters-long

LLM_PROVIDER=gemini
LLM_MODEL=gemini-2.0-flash    # or gemini-2.5-flash, gemini-pro
LLM_API_KEY=your_google_api_key
